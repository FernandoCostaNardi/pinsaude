name: Release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Release pipeline
        run: echo "Deploy pipeline — not yet implemented. Tag ${{ github.ref_name }}"
