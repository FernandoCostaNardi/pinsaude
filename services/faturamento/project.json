{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "faturamento",
  "root": "services/faturamento",
  "sourceRoot": "services/faturamento/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tools\\scripts\\run.cmd tools/scripts/mvn-build.js services/faturamento",
        "cwd": "{workspaceRoot}"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tools\\scripts\\run.cmd tools/scripts/mvn-test.js services/faturamento",
        "cwd": "{workspaceRoot}"
      }
    }
  },
  "tags": ["scope:services", "type:backend"]
}
