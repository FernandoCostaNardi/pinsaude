spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/pinsaude
    username: svc_fiscal
    password: ${DB_PASSWORD_FISCAL:fiscal_dev}
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: fiscal
  flyway:
    schemas: fiscal
    default-schema: fiscal
    enabled: true
    locations: classpath:db/migration
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/pinsaude
server:
  port: 8081
management:
  endpoints:
    web:
      exposure:
        include: health,info
