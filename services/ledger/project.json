{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "ledger",
  "root": "services/ledger",
  "sourceRoot": "services/ledger/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tools\\scripts\\run.cmd tools/scripts/mvn-build.js services/ledger",
        "cwd": "{workspaceRoot}"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tools\\scripts\\run.cmd tools/scripts/mvn-test.js services/ledger",
        "cwd": "{workspaceRoot}"
      }
    }
  },
  "tags": ["scope:services", "type:backend"]
}
