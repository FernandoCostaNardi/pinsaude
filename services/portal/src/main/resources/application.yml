spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/pinsaude
    username: svc_portal
    password: ${DB_PASSWORD_PORTAL:portal_dev}
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/pinsaude

server:
  port: 8087

management:
  endpoints:
    web:
      exposure:
        include: health,info
